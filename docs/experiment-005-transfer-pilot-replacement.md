# Experiment 005 transfer-pilot replacement amendment

The verified partition-52 attempt failed at worker startup before publishing a complete block or episode row. Partition 52 is permanently retired; partition 53 remains untouched. This additive amendment reserves fresh partition 54 without materializing it.

No scientific design field changes. The frozen cases, counts, ordering rule, metrics, thresholds, gates, replay selection, and descriptive-only claim boundary remain byte-anchored to the original transfer-pilot design. The amendment changes only the seed namespace and process-launch mechanics.

The replacement runner uses an explicit multiprocessing `spawn` context behind an importable module CLI. Partition-951 fixture runs establish byte-identical serial/two-process output, deterministic replay, and checkpoint continuation before the amendment is frozen. Atomic content-hashed shards, canonical assembly, duplicate/corruption detection, zero retries, zero replacement roots, and fail-closed incomplete-campaign behavior are preserved.

The frozen readiness artifact records whether the amendment is READY for a later separate write-once materialization and execution. This amendment task does not create partition-54 seeds or results and does not execute the replacement pilot.
