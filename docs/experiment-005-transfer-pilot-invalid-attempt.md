# Experiment 005 partition-52 transfer-pilot invalid attempt

The frozen partition-52 noninferential transfer pilot was materialized once. The seed manifest
contains the prospectively fixed 20 complete paired blocks and 40 planned episodes, including the
10-block replay subset.

The single authorized process-pool campaign invocation ended in a terminal worker-start
infrastructure failure before any complete block was published. The materialized seed bytes are
archived under `results/experiment-005-transfer-pilot/materialized-seeds/`; keeping the canonical
result path present prevents a second materialization. The immutable campaign identity and terminal
failure record are retained under `results/experiment-005-transfer-pilot/invalid-attempt-evidence/`.
No episode output exists.

Under the frozen conjunctive gates, the pilot is **invalid**:

- the zero-infrastructure-failure gate fails;
- exact completeness is not met;
- truth validity, estimator covariance, truth-event geometry, domain activation, descriptive
  mismatch evidence, and deterministic replay are not evaluated;
- no partial outcome was available or used;
- no retry, continuation, replacement root, extension, imputation, or replay was performed; and
- no superiority, noninferiority, hazard-rate, ranking, or architecture-benefit claim is supported.

Partition 52 is not reusable. Partition 53 remains untouched and has no confirmatory question,
sample size, design, generator, seed, or result.

The smallest next task is a separate prospective replacement-pilot execution amendment. It must
provide an importable process-pool entry point, phase-aware validation, and a fresh disjoint
diagnostic partition while preserving the scientific design. That task must not use partition 53.
