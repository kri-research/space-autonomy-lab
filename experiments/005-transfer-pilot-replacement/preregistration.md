# Experiment 005 transfer-pilot replacement execution amendment

_Pre-execution amendment freeze; partition 54 remains reserved and unmaterialized. Partition 53 remains untouched._

## Validity boundary

The single authorized partition-52 pilot attempt is permanently invalid and non-reusable. Its frozen closeout records zero completed blocks, zero durable episode rows, one terminal `BrokenProcessPool`, no replay, no retry, no replacement root, and no evaluated scientific endpoint. No partition-52 outcome informed this amendment because no completed outcome existed.

This amendment changes execution identity only. It preserves the frozen ten cases, two diagnostic configurations, two roots per case, 20 complete paired blocks, 40 episodes, 10-block replay subset, deterministic model and controller code, stochastic stream derivation, metrics, thresholds, conjunctive acceptance gates, descriptive-only analysis boundary, and prohibition on scientific or architecture claims. Partition 52 is retired. Partition 53 remains reserved and untouched. Following the repository replacement convention while skipping reserved partition 53, partition 54 is the next unused disjoint diagnostic namespace.

## Portable execution path

The only authorized live entry point is an importable module invocation:

```text
uv run python -m kri_space_autonomy.experiment_005_transfer_pilot_replacement.workflow execute --workers 8
```

The workflow explicitly requests Python's `spawn` multiprocessing context. Stdin programs, `python -c`, heredocs, and other ephemeral `__main__` entry points are not authorized for a live replacement campaign. Multi-core process execution is retained.

Each frozen paired root block remains one work unit. Completed blocks are published as atomic, content-hashed checkpoint shards bound to one immutable campaign identity. Worker completion order cannot affect output order: final rows are assembled only by ascending frozen block index and within-block run order. Existing shards are hash-checked before use; corrupt, foreign, duplicate, noncanonical, incomplete, or inconsistent evidence fails closed. A later invocation may continue only missing unpublished blocks from the identical frozen schedule after validating all completed shards. It may not recompute completed blocks, retry a terminally failed block, substitute a seed or root, extend the schedule, or alter any scientific field.

## Fixture-only portability validation

Before this freeze, the module entry point is exercised only with partition-951 deterministic fixtures. Fresh serial, two-process spawn, checkpoint-continuation, and replay runs must produce byte-identical canonical episode rows. Corrupt and terminal-failure fixtures must fail closed. Partition 54 is neither derived into a live manifest nor executed during amendment validation.

## Prospective execution decision

A READY decision authorizes a later, separate write-once partition-54 materialization and module-based execution. It does not materialize the partition, run the pilot, evaluate a scientific endpoint, or support a scientific claim.
