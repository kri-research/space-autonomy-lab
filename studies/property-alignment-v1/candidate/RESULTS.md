# Developmental results

The sealed execution completed 24 named information contexts, 48 boundary-grid cases, 8,704 exact scalar command checks and 10 individual 300s recovery continuations. No selected run was replaced, and no protected evaluation or physical experiment was accessed.

| Evidence group | Checked common prefix | Checked obstruction | Unresolved |
| --- | ---: | ---: | ---: |
| Named contexts |18|4|2|
| Boundary grid |30|18|0|

The three-state HCW case has a positive exact residual certificate supported by three positional inequalities. Each of its three pairs has a validated common one-second action. All five individual states used in the triple and opposed-radial examples separately complete contained 300s trajectories with qualifying hold in both HCW and nonlinear continuations. Their existence witnesses do not form one shared policy for the ambiguous set.

The individual stopping-score ablation returns nonnegative scores for the opposed-radial and three-state conflicts, while the full shared-action criterion proves an obstruction. The mean-only shortcut certifies its fictional point; the full-set check does not certify that action. These shortcuts are deliberately invalid approximations, not descriptions of the intact protective baselines.

The correlated-union example permits a common zero-input prefix. Its enclosing box cannot certify the same prefix because it contains spurious positions outside the nonconvex admissible set. That outer-only box cannot supply attainable witnesses to the negative test. The outer-halfspace control remains unresolved rather than falsely safe.

The combined uncertainty, age, delay and effectiveness case has a checked obstruction; each individually applied limitation permits a prefix. Removing three of the four factors gives a prefix within the run budget. The effectiveness-loss removal is retained as a budget miss: the first uncertain zero-action check took the complete call beyond the 1 s budget, with 2.5515 s recorded and no command returned. The algorithm was not retuned or rerun to convert this observation into a success. A late calculation is not a hardware-safe fallback.

The native-design baseline comparison keeps the Task 05 implementations and settings unchanged. Their four protected outputs all pass the independent prefix check. Nineteen information contexts admit evaluation of both baselines; five reduced-effectiveness contexts are outside the implemented native actuation bounds. Large hulls may violate sufficient terminal/derivative domains. No abstention is interpreted as a theorem failure, and no like-for-like recovery advantage is claimed for a prefix-only method.

A separately recorded analytical clarification derives the exact recoverable blind duration for a symmetric deterministic scalar pair. It is 1/4 s, although its longest common safe prefix is 1/2 s. Exact recovery trajectories and the full convexity/stopping-distance argument are included. This known viability specialization demonstrates why prefix duration must not be reported as recovery duration; it does not change the original matrix.

The strongest surviving result is the checked concrete HCW multi-hypothesis obstruction and the two-sided diagnostic distinction between a safe prefix, genuine first-command incompatibility and insufficient evidence. Generic observational incompatibility, stopping distances and linear alternatives are prior mathematics, including the close recent preprint recorded in `method_sources.json`. Broad novelty remains unestablished. `claim_limits.json` fixes the scope for subsequent evaluation.
